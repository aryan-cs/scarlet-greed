class Renderer {

    constructor (spritesheet, spriteWidth, spriteHeight, spriteCount, frameDelay, loop, scale) {

        this.spritesheet = spritesheet;
        this.spriteWidth = spriteWidth;
        this.spriteHeight = spriteHeight;
        this.spriteCount = spriteCount;
        this.frameDelay = frameDelay;
        this.loop = loop;
        this.scale = scale;

    }

    display (state) {

        if (state === "idle") {

        }

        if (state === "walk") {
            
        }
        
        if (state === "die") {

        }

        return frame;

    }
    
    

  }